package:
  name: worker-build
  version: 0.8.5
  epoch: 0
  description: build Rust Cloudflare Workers into WASM binaries.
  copyright:
    - license: Apache-2.0
  dependencies:
    runtime:
      - wasm-pack
      - wasm-bindgen

environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/os
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - openssl-dev

pipeline:
  - uses: git-checkout
    with:
      expected-commit: cc174db40ba9f648623805e98053a4eba3bb50c0
      repository: https://github.com/cloudflare/workers-rs
      tag: v${{package.version}}
      recurse-submodules: true
  - uses: cargo/build
    with:
      modroot: worker-build
      output: ${{package.name}}
